version: 2.1

orbs:
  hmpps: ministryofjustice/hmpps@1.1.1

executors:
  builder:
    docker:
      - image: circleci/node:12-buster-browsers
    working_directory: ~/app

jobs:
  build:
    executor: builder
    steps:
      - checkout
      - restore_cache:
          key: node_modules-{{ checksum "package-lock.json" }}-{{ checksum "package.json" }}
      - run:
          name: Install Dependencies
          command: npm ci --no-audit
      - save_cache:
          key: node_modules-{{ checksum "package-lock.json" }}-{{ checksum "package.json" }}
          paths:
            - ./node_modules
      - run:
          name: Run eslint
          command: npm run lint
      - run:
          name: Run Tests
          command: npm run test
      - persist_to_workspace:
          root: .
          paths:
            - node_modules

workflows:
  version: 2
  build-test-and-deploy:
    jobs:
      - build:
          filters:
            tags:
              ignore: /.*/
      - hmpps/build_docker:
          name: build_docker
          image_name: "quay.io/hmpps/prison-services-feedback-and-support"
          filters:
            branches:
              only:
                - main
      - hmpps/deploy_env:
          name: deploy_dev
          env: "dev"
          requires:
            - build_docker
          filters:
            branches:
              only: main
      - request-preprod-approval:
          type: approval
          requires:
            - deploy_dev
      - hmpps/deploy_env:
          chart_name: prison-services-feedback-and-support
          release_name: prison-services-feedback-and-support
          name: deploy_preprod
          env: "preprod"
          context: prison-services-feedback-and-support-preprod
          requires:
            - request-preprod-approval
      - request-prod-approval:
          type: approval
          requires:
            - deploy_preprod
      - hmpps/deploy_env:
          chart_name: prison-services-feedback-and-support
          release_name: prison-services-feedback-and-support
          name: deploy_prod
          env: "prod"
          slack_notification: true
          context: prison-services-feedback-and-support-prod
          requires:
            - request-prod-approval


  scheduled:
    triggers:
      - schedule:
          cron: "0 7 * * 1-5"
          filters:
            branches:
              only:
                - main
    jobs:
      - hmpps/npm_security_audit